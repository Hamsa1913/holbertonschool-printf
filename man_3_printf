.TH _PRINTF 3 "Holberton School" "Custom printf"
.SH NAME
_printf \- custom implementation of the standard printf function
.SH SYNOPSIS
.B #include "main.h"
.br
.BI "int _printf(const char *" format ", ...);"
.SH DESCRIPTION
The _printf function produces output according to a format string.
It supports a subset of the standard printf conversion specifiers.

For this version, the following conversion specifiers are implemented:

.TP
.B %c
Prints a single character.

.TP
.B %s
Prints a null-terminated string.

.TP
.B %%
Prints a literal percent sign.

.TP
.B %d
Prints a signed integer in base 10.

.TP
.B %i
Prints a signed integer in base 10.

Unsupported specifiers are printed as a percent sign followed by the character.

The function uses only the write() system call and standard variadic macros.
No global variables are used.

.SH RETURN VALUE
_printf returns the total number of characters printed.
If the input format is NULL or ends with a lone percent sign, it returns -1.

.SH EXAMPLES
.nf
_printf("Number: %d\n", 98);
_printf("String: %s\n", "Holberton");
_printf("Percent: %%\n");
_printf("Integer: %i\n", -402);
.fi

.SH AUTHOR
Written by:
Hamsa Alammar & Abdulrahman Alqadhibi

